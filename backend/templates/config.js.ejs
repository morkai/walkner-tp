window.appLocale = window.LOCALE = 'pl';

localStorage.setItem('LOCALE', window.appLocale);

require = {
  baseUrl: '/',
  paths: <%- paths %>,
  shim: <%- shim %>,
  config: {
    i18n: {
      locale: appLocale
    }
  }
};
